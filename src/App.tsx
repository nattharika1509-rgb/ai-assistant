import { Console } from './components/Console';
import './App.css';

function App() {
  return (
    <main style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", width: "100%" }}>
      <Console />
    </main>
  );
}

export default App;
